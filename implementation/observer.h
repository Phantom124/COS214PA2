#ifndef OBSERVER_H
#define OBSERVER_H

#include "string"
using namespace std;
class observer{
    public:
    void update(string update);
    observer();//for the operation() in the uml ?
    private:

};

#endif //OBSERVER_H